build/
build_t_embed/
build_test/
build_s3/
.cache/
.idea/
__pycache__/
sdkconfig.old
.DS_Store
build_waveshare_c6/
.vscode/settings.json

# Local-dev tooling (not for upstream)
_flash_helper.py

# Managed components are pulled by IDF component manager from idf_component.yml
# + dependencies.lock; the source-of-truth is those two files.
managed_components/

# Local development snapshots and generated release artifacts
.backups/
*.before-*
*.after-*
*.bak
*.orig
*.rej
*~
releases/*.zip
*-backup-*
*.backup-*

# Host-side unit test build output (tests/host/run_host_tests.bat)
build_host/

# Assembled SD card payload (generated from the Momentum checkout)
sdcard/
/tmp/
build_t_embed_200/
build_upgrade_200.log

# --- v2 push hygiene: keep build artifacts, caches, logs and dumps out of git ---
# All ESP-IDF build directories (build_multiboot/, build_ir_verify/, build_t_embed_release/, ...)
build_*/
# ccache store
.ccache/
# Every log file anywhere
*.log
# On-device dumps / merged app images left at the repo root
/*.bin
# Local test-flash and release output trees
latest_tested/
releases/
# Root SD payload zips (sdcard.zip, "sdcard (2).zip")
/sdcard*.zip
# Generated / variant sdkconfigs (real defaults stay tracked)
sdkconfig.generated_by_claude_*
sdkconfig.variant_*
# Local migration bookkeeping
migration_*audit.json
# Gradle/IDE caches from vendored companion apps under applications_user/
.gradle/
node_modules/
*.iml
.externalNativeBuild/
.cxx/
