# Release table for the 16 MB T-Embed CC1101 — single firmware, no dual boot.
#
# ota_0 is gone: Dual Boot is disabled in public builds, so a 5 MB slot for a
# second firmware is dead space.
#
# There is deliberately NO `assets` partition. It used to hold a tar of the SD
# content this port adds, installed on first boot -- but carrying it meant the
# merged image spanned 0x0..0xa40000, so a flash was 8.5 MB of mostly 0xFF
# padding. Dropping it puts the merged image at ~3.3 MB, which matters most for
# the web flasher: a smaller transfer over USB-CDC is a faster and far more
# reliable flash. Users get the SD content from the release zip instead.
#
# components/storage/asset_installer.c is still compiled and is a deliberate
# no-op here: it calls esp_partition_find_first(..., "assets") and returns
# immediately when the partition is absent. Keeping it costs nothing and means
# reinstating the partition is a one-line change to this file.
#
# factory is 4 MB. The app is ~3.03 MB (76%), so this is the smallest power-of-
# two that fits -- 3 MB is about 29 KB too small and will fail the build.
#
# otadata is kept even though nothing writes an OTA slot: the bootloader-level
# escape hatch (CONFIG_BOOTLOADER_FACTORY_RESET) erases it to force factory,
# and an absent/erased otadata boots factory anyway. 8 KB is not worth the risk.
#
# For a dual-boot build, point CONFIG_PARTITION_TABLE_CUSTOM_FILENAME back at
# partitions_singleapp_16mb.csv — that one MUST stay byte-identical to
# Bruce-Firmware/custom_16Mb_dualboot.csv.
#
# Ends exactly at 0x1000000 (16 MB).
#
# Name,     Type, SubType,  Offset,    Size,     Flags
nvs,        data, nvs,       0x9000,    0x6000,
phy_init,   data, phy,       0xf000,    0x1000,
otadata,    data, ota,       0x10000,   0x2000,
factory,    app,  factory,   0x20000,   0x400000,
coredump,   data, coredump,  0x420000,  0x20000,
spiffs,     data, spiffs,    0x440000,  0xbc0000,
