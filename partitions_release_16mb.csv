# Release table for the 16 MB T-Embed CC1101 - single firmware, no dual boot.
#
# Firmware only. The SD card is set up by hand from sdcard.zip, so nothing is
# carried in flash for it: no `assets` partition, and the merged image is just
# bootloader + table + app. That keeps the download and the flash small.
#
# factory is 4 MB against a ~3.0 MB image (about 25% headroom). It was 8 MB
# while the SD payload shipped in flash; without that there is no reason to
# reserve space the app will not use, and everything freed goes to spiffs.
#
# otadata is kept even though nothing writes an OTA slot: the bootloader-level
# escape hatch (CONFIG_BOOTLOADER_FACTORY_RESET) erases it to force factory,
# and an absent/erased otadata boots factory anyway. 8 KB is not worth the risk.
#
# Name,     Type, SubType,  Offset,    Size,     Flags
nvs,        data, nvs,       0x9000,    0x6000,
phy_init,   data, phy,       0xf000,    0x1000,
otadata,    data, ota,       0x10000,   0x2000,
factory,    app,  factory,   0x20000,   0x400000,
coredump,   data, coredump,  0x420000,  0x20000,
spiffs,     data, spiffs,    0x440000,  0xbc0000,
